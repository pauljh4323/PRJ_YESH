// Static, non-editable instructional/decorative text box.
// Not an input field — does not affect game logic.
// Copy is placeholder only; final copy is TBD (see context.md "Open items").
function TextBox() {
  return (
    <div className="text-box">
      <p>텍스트 상자</p>
    </div>
  )
}

export default TextBox
